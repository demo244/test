  when(headersSpec.exchangeToMono(any())).thenAnswer(invocation -> {
            Function<ClientResponse, Mono<ResponseWrapper<AricResponse>>> func = invocation.getArgument(0);
            ClientResponse mockResponse = ClientResponse.create(HttpStatus.OK).body("").build();
            return func.apply(mockResponse);
        });
