/**
 * A custom HttpServletRequestWrapper that reads and caches the request body
 * so it can be read multiple times.
 * <p>
 * This is useful when the request body needs to be logged or processed
 * before it is consumed by the filter chain or controller.
 */
